services:
  app:
    build: .
    env_file:
      - .env
    environment:
      - TZ=Asia/Taipei
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
    command: ["poetry", "run", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
